# Firmware

The firmware is deployed directly to each node. The primary function is motion detection and automatic light switching based on detected activity. But it also supports bidirectional communication with the gateway over TCP/IP, which allows the server to control the light remotely, modify internal configuration and receive status updates.
