# Server

This module contains the main server. It consists of a UI and an API. The UI is used for node registration, individual node control, and reporting through dashboards. The API handles state reporting from the end nodes.
