{
  "name": "gateway",
  "version": "1.0.0",
  "description": "The gateway is a physical middleware layer that manages a cluster of connected end nodes. It reads messages from the MQTT broker, distributes payloads to end devices, and serves as a proxy for communication between the end devices and the server.",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "consume": "ts-node src/consumer.ts",
    "produce": "ts-node src/producer.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@types/node": "^24.5.2"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "mqtt": "^5.14.1",
    "ts-node": "^10.9.2"
  }
}